import { NativeModules } from 'react-native'; const NativeMap = NativeModules.MapySdkModule || null; export class MapSdkBridge { static initialize(apiKey){ if (NativeMap && NativeMap.initialize) { try { NativeMap.initialize(apiKey || ''); } catch(e){ console.warn('Map SDK initialize failed', e); } } else { console.warn('Mapy.cz native module not installed — using deeplink fallback.'); } } static setDestination(lat,lon,label){ if (NativeMap && NativeMap.setDestination) { NativeMap.setDestination(lat,lon,label || ''); } else { const url = f"https://mapy.cz/zakladni?x={lon}&y={lat}&z=14"; console.log('Open fallback map URL:', url); } } }